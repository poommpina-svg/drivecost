# ตั้งค่าระบบบัญชี DriveCost v2.5

หน้าแอพสำหรับผู้ใช้มีเพียง:

- สมัครสมาชิก
- เข้าสู่ระบบ
- ลืมรหัสผ่าน
- ออกจากระบบ
- บันทึกข้อมูลอัตโนมัติ

รายละเอียดการซิงก์และฐานข้อมูลทำงานอยู่เบื้องหลัง

## 1. สร้าง Supabase Project

สร้าง Project แล้วเก็บค่าต่อไปนี้:

- Project URL
- Publishable key (`sb_publishable_...`) หรือ legacy `anon` key

ห้ามใช้ `sb_secret_...` หรือ `service_role` key ในหน้าเว็บ

## 2. สร้างฐานข้อมูล

เปิด SQL Editor แล้วรันไฟล์:

```text
supabase/schema.sql
```

ไฟล์นี้สร้างตารางโปรไฟล์ ตารางข้อมูลผู้ใช้ ฟังก์ชันบันทึก revision และ Row Level Security

## 3. ปิดการยืนยันอีเมล

ในการตั้งค่า Email provider ของ Supabase ให้ปิด `Confirm email`

เมื่อปิดแล้ว ผู้ใช้จะสมัครและเข้าสู่ระบบได้ทันที โดยไม่ต้องกดลิงก์ยืนยันอีเมล

## 4. ตั้งค่าลืมรหัสผ่าน

แม้จะปิดการยืนยันอีเมล ระบบลืมรหัสผ่านยังต้องส่งอีเมล จึงควรตั้งค่า SMTP ของ Project ให้พร้อมใช้งาน

ตั้ง Site URL และ Redirect URL ให้ตรงกับ URL ของแอพ เช่น:

```text
http://127.0.0.1:8080
http://127.0.0.1:8080/**
```

เมื่อนำขึ้นระบบจริง ให้เปลี่ยนเป็นโดเมน HTTPS ของแอพ

## 5. เชื่อมแอพ

แก้ไฟล์ `supabase-config.js`:

```javascript
window.DRIVECOST_SUPABASE_CONFIG = {
  url: "https://YOUR_PROJECT.supabase.co",
  publishableKey: "sb_publishable_YOUR_KEY"
};
```

หรือกรอกผ่านส่วน “การตั้งค่าสำหรับผู้พัฒนา” ในหน้า Account ครั้งแรก

## 6. เปิดแอพ

```bash
npm start
```

แล้วเปิด:

```text
http://127.0.0.1:8080
```

## พฤติกรรมหลังเข้าสู่ระบบ

- บันทึกข้อมูลอัตโนมัติเมื่อสถานการณ์ ประวัติ ราคา หรือการตั้งค่าเปลี่ยน
- เมื่อออฟไลน์ ระบบเก็บรายการรอไว้ใน IndexedDB
- เมื่อกลับมาออนไลน์ ระบบส่งข้อมูลเข้าบัญชีเอง
- หากเปิดอีกอุปกรณ์และเข้าสู่บัญชีเดิม ระบบโหลดข้อมูลเดิมกลับมา
- หากพบข้อมูลจากบัญชีอื่น ระบบจะถามก่อนและไม่รวมข้อมูลเอง


## การแยกข้อมูลในเครื่อง v2.6.0

แอพแยก localStorage ด้วย Supabase user ID โดยอัตโนมัติ บัญชีหนึ่งจึงไม่อ่าน
สถานการณ์ ประวัติ ราคา การตั้งค่า หรือร่างคำนวณของอีกบัญชีในเบราว์เซอร์เดียวกัน

การแยก Cloud ยังคงบังคับด้วย `auth.uid()` และ Row Level Security ใน
`supabase/schema.sql`
