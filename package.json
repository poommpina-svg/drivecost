{
  "name": "drivecost-production",
  "version": "3.1.3",
  "private": true,
  "description": "DriveCost production web service for Render with Supabase and an administrator integrity center",
  "license": "UNLICENSED",
  "scripts": {
    "start": "node server.js",
    "check": "node scripts/check-js.js",
    "test": "node --test tests/*.test.js",
    "smoke": "node scripts/smoke-test.js",
    "preflight": "node scripts/preflight.js",
    "verify": "npm run check && npm test && npm run smoke && npm run preflight",
    "audit:prod": "node scripts/production-audit.js"
  },
  "engines": {
    "node": "24.14.1"
  }
}
