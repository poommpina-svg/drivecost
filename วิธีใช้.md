# วิธีใช้แบบง่าย

## Windows

1. แตกไฟล์ ZIP ก่อน
2. ติดตั้ง Node.js 18 ขึ้นไป
3. ดับเบิลคลิก `เปิดแอพ.bat`
4. รอให้เบราว์เซอร์เปิด `http://127.0.0.1:8080`
5. ระบบจะดึงราคาน้ำมันให้อัตโนมัติ ไม่ต้องกด “อัปเดตราคาตอนนี้” ซ้ำ

## macOS

1. แตกไฟล์ ZIP
2. ติดตั้ง Node.js 18 ขึ้นไป
3. ดับเบิลคลิก `เปิดแอพ.command`
4. หาก macOS บล็อก ให้คลิกขวาไฟล์แล้วเลือก Open

## สถานะที่ควรเห็น

- จุดเขียว + “ข้อมูลสด” = ใช้ราคาจาก OR
- จุดน้ำเงิน + “แคชล่าสุด” = ใช้ราคาที่บันทึกไว้
- จุดส้ม = ข้อมูลเก่าหรือกำลังตรวจสอบ
- จุดแดง = ดึงราคาสดไม่ได้ แต่ยังกรอกราคาเองและคำนวณได้

## ข้อสำคัญ

อย่าเปิด `index.html` โดยตรงหากต้องการราคาสด เพราะไฟล์ HTML อย่างเดียวไม่มี server proxy
